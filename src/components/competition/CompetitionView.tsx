import { useState } from 'react'
import { AlertTriangle, Check, Clock3, Flag, LockKeyhole, Play, Shield, Trophy, X } from 'lucide-react'
import { useArena } from '@/components/app-provider'
import { useTimer } from '@/hooks/use-timer'
import { Badge, Button, Panel, ProgressBar } from '@/components/ui'

const modes = [{ name: 'Sprint rapide', duration: 30, detail: 'Une tâche ciblée. Un retour rapide.', xp: 150 }, { name: 'Challenge standard', duration: 60, detail: 'Un brief frontend complet avec le temps de tester.', xp: 250 }, { name: 'Simulation de compétition', duration: 120, detail: 'La boucle complète sous contrainte, sans indice.', xp: 400 }, { name: 'Entraînement complet', duration: 180, detail: 'Construire, tester, revoir et rédiger la transmission.', xp: 600 }]
const tasks = ['Structure du projet', 'Composants principaux', 'Responsive', 'Checklist accessibilité', 'Tests finaux']

export function CompetitionView() {
  const { settings, updateSettings } = useArena()
  const [mode, setMode] = useState(modes[2])
  const [started, setStarted] = useState(false)
  const [done, setDone] = useState<number[]>([0, 1, 2])
  const timer = useTimer(mode.duration, `competition:${mode.duration}`)
  const toggle = (index: number) => setDone((current) => current.includes(index) ? current.filter((item) => item !== index) : [...current, index])
  if (!started) return <div className="panel-in space-y-6"><div><p className="mb-2 text-[10px] font-semibold uppercase tracking-[.2em] text-[#5b8fc8]">Compétition / conditions contrôlées</p><h1 className="text-[26px] font-semibold tracking-[-.05em] text-white">La compétition commence avec le brief.</h1><p className="mt-1.5 max-w-[60ch] text-[12px] leading-5 text-[#8ca6c2]">Choisis une durée, verrouille les règles, puis suis la checklist comme pendant une vraie session chronométrée.</p></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-4">{modes.map((item) => <button type="button" key={item.name} onClick={() => setMode(item)} className={`focus-ring surface-panel-flat rounded-[9px] p-4 text-left transition-all ${mode.name === item.name ? 'border-[#438fe9] bg-[#123158] shadow-[inset_0_1px_0_rgba(111,180,255,.18)]' : 'hover:border-[#3a6289]'}`}><div className="flex items-center justify-between"><span className="grid h-8 w-8 place-items-center rounded-[6px] bg-[#173c65] text-[#6cb1ff]"><Clock3 size={16} /></span>{mode.name === item.name ? <span className="h-2 w-2 rounded-full bg-[#65aeff]" /> : null}</div><h2 className="mt-5 text-[14px] font-semibold text-white">{item.name}</h2><p className="mt-1 text-[11px] leading-5 text-[#849fba]">{item.detail}</p><div className="mt-4 flex items-center justify-between border-t border-[#294966] pt-3 text-[10px]"><span className="font-mono text-[#d5e4f3]">{item.duration} min</span><span className="text-[#f4cc57]">+{item.xp} XP</span></div></button>)}</div><div className="grid gap-4 xl:grid-cols-[1fr_.8fr]"><Panel title="Règles de compétition" action={<button type="button" aria-label="Activer ou désactiver les règles de compétition" aria-pressed={settings.competitionRules} onClick={() => updateSettings({ competitionRules: !settings.competitionRules })} className={`focus-ring relative h-5 w-9 rounded-full ${settings.competitionRules ? 'bg-[#2778dc]' : 'bg-[#304861]'}`}><span className={`absolute top-1 h-3 w-3 rounded-full bg-white transition-transform ${settings.competitionRules ? 'translate-x-5' : 'translate-x-1'}`} /></button>}><div className="p-4"><div className="flex gap-3 rounded-[7px] border border-[#3a5d7e] bg-[#122a45] p-3"><Shield size={17} className="mt-0.5 shrink-0 text-[#70b6ff]" /><div><p className="text-[11px] font-semibold text-[#dceafa]">{settings.competitionRules ? 'Les règles sont verrouillées pour cette session.' : 'Les règles d’entraînement sont assouplies.'}</p><p className="mt-1 text-[10px] leading-4 text-[#8ca8c3]">{settings.competitionRules ? 'Pas d’indice, de solution, d’aide IA, d’explication ni de correction avant la fin du minuteur.' : 'Tu peux consulter le brief et utiliser la bibliothèque pendant ton travail.'}</p></div></div></div></Panel><Panel className="flex flex-col justify-between"><div className="p-4"><Badge tone="yellow"><Flag size={11} /> Prêt à démarrer</Badge><h2 className="mt-4 text-[18px] font-semibold tracking-[-.03em] text-white">{mode.name}</h2><p className="mt-1 text-[11px] text-[#829db8]">{mode.duration} minutes · {mode.xp} XP à la fin</p></div><div className="border-t thin-rule p-4"><Button className="w-full" onClick={() => { setStarted(true); timer.start() }}><Play size={14} fill="currentColor" /> Démarrer la compétition</Button></div></Panel></div></div>
  return <div className="panel-in space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="mb-2 text-[10px] font-semibold uppercase tracking-[.2em] text-[#5b8fc8]">Compétition CodeArena</p><h1 className="text-[26px] font-semibold tracking-[-.05em] text-white">{mode.name}</h1><p className="mt-1.5 text-[12px] text-[#8ca6c2]">Concentre-toi d’abord. Revois ton travail quand le temps est écoulé.</p></div><Button variant="danger" onClick={() => { timer.pause(); setStarted(false) }}><X size={14} /> Abandonner la session</Button></div><div className="grid gap-4 xl:grid-cols-[1.2fr_.8fr]"><Panel className="overflow-hidden"><div className="grid gap-4 border-b thin-rule p-5 sm:grid-cols-[1fr_auto] sm:items-center"><div><p className="text-[10px] uppercase tracking-[.18em] text-[#7194b8]">Temps restant</p><p className="mt-2 font-mono text-[44px] font-semibold tracking-[-.07em] text-white">{timer.formatted}</p><div className="mt-3 flex items-center gap-2"><span className="signal-pulse h-2 w-2 rounded-full bg-[#54d2af]" /><span className="text-[10px] text-[#86a3be]">Règles de compétition : {settings.competitionRules ? 'actives' : 'désactivées'}</span></div></div><div className="grid h-28 w-28 place-items-center rounded-full border-[8px] border-[#1b3f66] border-t-[#58a6ff] text-center"><span className="font-mono text-[18px] text-[#cfe4fb]">{Math.round((done.length / tasks.length) * 100)} %<small className="block text-[9px] text-[#7896b4]">terminé</small></span></div></div><div className="p-5"><div className="flex items-center justify-between"><div><h2 className="text-[13px] font-semibold text-[#eaf3fc]">Checklist des tâches</h2><p className="mt-1 text-[10px] text-[#7895b1]">Garde la ligne d’arrivée en vue.</p></div><span className="font-mono text-[11px] text-[#a8c2dd]">{done.length} / {tasks.length}</span></div><ProgressBar value={(done.length / tasks.length) * 100} color="#4bcbae" className="mt-4 h-2" /><div className="mt-5 space-y-2">{tasks.map((task, index) => <button type="button" key={task} onClick={() => toggle(index)} className="focus-ring flex w-full items-center gap-3 rounded-[6px] border border-[#274864] bg-[#10283f] px-3 py-3 text-left hover:border-[#3e709b]"><span className={`grid h-5 w-5 place-items-center rounded-[4px] border ${done.includes(index) ? 'border-[#2a9f7d] bg-[#1b805f] text-white' : 'border-[#4b6a86] text-transparent'}`}>{done.includes(index) ? <Check size={13} /> : <span className="h-1.5 w-1.5 rounded-full bg-[#55738f]" />}</span><span className={`text-[11px] ${done.includes(index) ? 'text-[#75ae9c] line-through' : 'text-[#cad8e7]'}`}>{task}</span>{done.includes(index) ? <span className="ml-auto text-[9px] uppercase tracking-[.12em] text-[#5ed4b0]">fait</span> : null}</button>)}</div></div></Panel><Panel title="Contraintes du brief"><div className="p-5"><div className="flex gap-3"><LockKeyhole size={17} className="mt-0.5 shrink-0 text-[#f2ca52]" /><p className="text-[11px] leading-5 text-[#a6bbd0]">Ton espace d’aide reste volontairement silencieux. Note tes incertitudes, choisis une direction et avance. La revue commence quand le minuteur s’arrête.</p></div><div className="mt-5 rounded-[7px] border border-[#3f5f78] bg-[#142b42] p-3"><div className="flex items-center gap-2 text-[10px] font-semibold text-[#dbe8f6]"><AlertTriangle size={13} className="text-[#f2c64d]" /> Une règle utile</div><p className="mt-2 text-[10px] leading-4 text-[#86a2bd]">Quand une tâche est floue, écris ton hypothèse et teste d’abord la preuve la plus petite.</p></div></div></Panel></div><Panel><div className="flex flex-wrap items-center justify-between gap-3 p-4"><div className="flex items-center gap-3"><Trophy size={17} className="text-[#f0c94d]" /><div><p className="text-[12px] font-semibold text-white">Reste concentré. Tu peux le faire.</p><p className="mt-1 text-[10px] text-[#7f9bb7]">La fin déverrouille le bilan et les XP.</p></div></div><Button onClick={() => timer.pause()}><Trophy size={13} /> Terminer la compétition</Button></div></Panel></div>
}
